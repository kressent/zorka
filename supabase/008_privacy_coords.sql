-- ═══ ЗОРЬКА · приватность точных координат («секретные места») ═══════════════
-- Запусти в Supabase → SQL Editor. Идемпотентно.
-- Проблема: политика catches_public отдавала анониму ВСЕ колонки публичных строк,
-- включая точные lat/lon. Для рыболовки это утечка секретных точек.
-- Решение: закрыть чтение именно точных колонок для всех, кроме владельца строки.
-- Лента берёт огрублённые lat_pub/lon_pub через вью feed_trips (owner = postgres,
-- читает базу в обход этого) — она НЕ пострадает. Публикация (INSERT) тоже: право
-- на вставку колонок отдельное от чтения, триггер fuzz_coords работает как раньше.

revoke select (lat, lon) on catches from anon, authenticated;
revoke select (lat, lon) on spots   from anon, authenticated;

-- Прочие поля публичного улова (вид, вес, водоём) остаются открытыми — это и есть
-- лента, тут секрета нет. Закрыты только точные координаты. Публикация уловов и
-- работа приложения не затрагиваются (пишет и читает оно через свои пути).
