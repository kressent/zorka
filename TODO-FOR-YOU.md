# ✅ Что нужно от тебя (актуальное)

Сайт live: **https://kressent.github.io/zorka/**. Почти всё уже подключено и работает.
Ниже — только то, что реально осталось.

---

## 🚀 ПЕРВОЕ ДЕЛО — набор рыбаков (Башкирия, Telegram)
Готов весь арсенал → **`docs/LAUNCH-BASHKIRIA.md`** (пошагово: бот, канал, готовые посты
на неделю, шаблоны ВК/ОК, где брать людей). От тебя:
1. **Токен бота** от @BotFather → мне (или запусти `bot/bot.js`). Инструкция в доке.
2. **Создать Telegram-канал** + описание + закреп (текст готов).
3. **Флаер** для магазинов/берега — `docs/flyer.html` (открыть → вставить QR → печать).

## 🌙 Что я сделал ночью (готово, в проде)
- **Прогноз учится на уловах:** вид, который реально ловят рядом, получает «✓ ловят рядом»
  и поднимается при равном балле (`calibrate.js`). Первый слой обучения на данных.
- **«Рыба по водоёму» ожила даже без уловов:** справочник водоёмов Башкирии (Павловка,
  Нугуш, Белая, Аслыкуль…), честно помечен «справочно».
- **«🎣 Позвать друга»** в профиле и пустой ленте (рост через сарафан).
- **Affiliate готов** (выключен) — `docs/MONETIZATION.md`, включается одной строкой в конфиге.
- **Доки под сторы/бренд/платформу:** `privacy.html`, `STORE-LISTING.md`, `TRADEMARK.md`
  (ТЗ 9/42), `ICONS.md`, `PLATFORM.md` (PWA vs Flutter), `YANDEX-MAPS.md`, `PHOTO-PLAN.md`.
- **Фото улова** — не делал вслепую (нужен тест камеры на телефоне), готов план — сделаем вместе.

## 🟢 Сделано (можно вычеркнуть)
- ✅ **Все миграции прогнаны** (009–016): комментарии, трофей, приманки, realtime,
  приватность координат (014), приманка в ленте (015), отметки воды (016).
- ✅ **Секретный ключ удалён** (безопасность).
- ✅ **SMTP настроен** — регистрация новых людей работает (порт 587 + доступ в Яндексе).
  Осталось разок пройти по ссылке из письма и убедиться, что вход после подтверждения
  проходит. Тест: инкогнито → почта с temp-mail.org или `твойгмейл+что-угодно@gmail.com`.

## ✅ Пуш на телефон — НАСТРОЕН И РАБОТАЕТ
VAPID-ключи, таблица (013), Edge-функция `push-notify`, триггеры (017) — всё живое.
Лайк/коммент к твоему улову → пуш прилетает. (На iPhone для пушей нужно один раз
«Поделиться → На экран Домой» и открывать с иконки.) Приватный VAPID-ключ хранится
в секретах Supabase — при желании перевыпустишь.

## 🆕 Прогнать миграции (SQL Editor) + 1 деплой
- **`018_reports.sql`** — жалобы на фейк/спам в ленте (антиспам). Идемпотентна.
  Без неё лента работает как раньше (кнопка 🚩 просто скажет «не удалось»).
- **Пуш «завтра жор»** (удержание, необязательно): `019_push_location.sql` +
  `020_forecast_cron.sql`, затем `supabase functions deploy forecast-push --no-verify-jwt`.
  Пошагово — в `docs/PUSH-SETUP.md` (раздел «Бонус: пуш завтра жор»). Без этого всё
  остальное работает; пуш о клёве просто не приходит.

## ⏸ Отложено (не горит)
- **Домен `nakryuchke.com`** (~1800 ₽/год) — взять позже, когда будет не жалко.
  `.ru`/`.рф`/кириллический `.com` заняты перекупщиками (250k — мимо). Пока сайт живёт
  на `kressent.github.io/zorka` (работает нормально). Как купишь — привяжу за 20 мин
  и перепишу все ссылки. Бюджетно на потом: `na-kryuchke.ru` / `nakryuchke.site` (~200–249 ₽).
- **Почта проекта** — красивую (info@nakryuchke.com) сделаем после домена; пока `zorka.klyov@yandex.ru`.

## 🤖 Бот/канал 24/7 — ГОТОВО (осталась 1 SQL-строка)
- ✅ Функции `telegram-bot` (webhook) и `telegram-daily` задеплоены на Supabase.
- ✅ Секреты заданы, webhook установлен, обе функции проверены (бот отвечает из облака,
  дайджест постится). Комп можно выключать. Локальный `node bot/bot.js` больше не нужен.
- 🟡 **Осталось:** прогнать `supabase/021_telegram_cron.sql` в SQL Editor — чтобы дайджест
  уходил в канал автоматически каждое утро (02:00 UTC ≈ 07:00 Уфа). Одна вставка.

## 🟡 Осталось — когда захочешь (по каждому проведу по шагам)
- **🔤 Название — ЗАФИКСИРОВАНО: «На крючке»** (классы МКТУ 9/42 свободны). Осталось по желанию:
  занять домены `накрючке.рф` / `nakryuchke.ru` и подать ТЗ в классах 9/42 (~3500 ₽ пошлина,
  помогу с заявкой). Ни то, ни другое — НЕ блокер запуска.
- **🖼 PNG-иконки** для сторов — перегенерировать из нового `icons/icon.svg` (онлайн-конвертер).
  В вебе иконка уже обновлена (заря + рыба).
- **🗺 Ключ Яндекс.Карт** — живой спутник (сейчас бесплатный Leaflet/Esri, и так неплохо).
- **🏪 RuStore / Google Play** — при публикации. Тексты карточки — в `docs/LAUNCH.md`.
- **🐙 GitHub-токен** `ghp_…` — им я выкладываю обновления. Перевыпустишь когда захочешь.

---

## 🎣 Полевой фидбэк (4 дня рыбалки) — я уже починил
Дата в дневнике (любой день), геопоиск с деревнями + геолокация по факту + метка на
карте, тап по будущему дню → что клюёт, **«здесь реально ловят» / «где ловится вид»**
(старт killer-фичи по водоёмам). Детали — `docs/ROADMAP.md` (Этап 3.5).

**Приоритет по твоему полю:** развивать «рыбу по водоёму» (что/где ловится по реальным
уловам) — за это готовы платить. Наполняется, когда рыбаки начнут вести дневники.

### Что нужно, чтобы это «ожило»
Больше **реальных уловов** в базе → зови первых рыбаков (для этого и настраивали SMTP).
`npm test` — 67 проверок зелёные. Подробности — `CLAUDE.md`, `docs/ROADMAP.md`.
