# CLAUDE.md — контекст проекта «Зорька» для ИИ-ассистента

> Этот файл — источник правды по проекту. Читай его первым. Он продублирован в
> репозитории специально, чтобы контекст не потерялся между сессиями.

## ⚠️ Правила
- Работать **только над этим проектом** (`E:\Зорька`). В другие папки/проекты не лезть.
- Папка раньше называлась `Рыболов`, переименована в `Зорька` 2026-07-24.
- **НАЗВАНИЕ ПРОДУКТА — «На крючке»** (зафиксировано 2026-08-01). «Зорька» осталась
  только как кодовое имя папки/репозитория (`E:\Зорька`, `kressent/zorka`,
  `kressent.github.io/zorka`) — их НЕ переименовываем (рабочие ссылки/установки).
  Во всех **видимых** пользователю местах — «На крючке». Выбор: классы МКТУ 9/42
  свободны, слово на слуху, двойной смысл «подсел/не оторваться» (хорошо для удержания).
- Интерфейс — **на русском**. Аудитория — РФ/СНГ, пресноводная рыбалка.

## Что это
Мобильное приложение: **прогноз клёва + дневник уловов + карта мест + снасти**.
Главный экран — «что тебя сегодня ждёт у воды»: рейтинг рыбы по убыванию клёва,
лучшие часы, на что ловить. Дальше — дневник, карта, комплекты снастей.

**Стратегическое ядро — «маховик данных»:** ценность не только в прогнозе по
приметам, а в том, что приложение **учится на реальных уловах пользователей**
(что/где/когда/на что поймали). Чем больше людей — тем точнее прогноз. Сейчас
это ещё не подключено (нет сервера); заложен фундамент.

## Текущее состояние (эта версия)
Рабочая **веб-PWA**, дизайн «Дневной альманах» (светлый). Реализовано:
- Прогноз на живой погоде **Open-Meteo** (без ключа), движок на экспертных правилах:
  луна в оценке, честный балл дня (из топ-рыб), **почасовой график клёва**, окна клёва,
  **вода «с памятью»** (60 дней осадков + тренд), **прогноз на 2 недели** + лучший день.
- 4 экрана: Прогноз, Дневник, Карта, Снасти. Офлайн (service worker, network-first + кэш).
- **Карточка улова** (`js/catchcard.js`) — виральный шэр (Web Share на HTTPS / скачивание PNG).
- Баннер нерестового запрета, honesty-строка, фирменная иконка (`icons/icon.svg`).
- **Автоматизация:** `js/postgen.js` (прогноз → готовый пост) + **Telegram-бот** в `bot/`
  (bot.js — отвечает прогнозом на город; daily.js — постер канала, `--dry` для теста).
  Готово к запуску, нужен токен @BotFather (за пользователем).
- **Бэкенд подготовлен:** `supabase/schema.sql` (таблицы уловов/мест/снастей + приватность
  + RLS) и `js/config.js` (пустые ключи). Приложение работает на `localStorage`, пока
  ключей нет; как пользователь даст URL+anon-key — подключаю облако/аккаунты (старт маховика).
- Маркетинг: `docs/MARKETING.md` (плейбук), `docs/LAUNCH.md` (готовый контент-пак).
- **`TODO-FOR-YOU.md`** — что нужно от пользователя (Supabase-ключи, токен бота, хостинг HTTPS,
  Яндекс-ключ, сторы). Всё, что можно без него, уже сделано.
- **ПОДКЛЮЧЕН СЕРВЕР (Supabase):** проект live, вход по **почте+паролю** (работает на
  компе/телефоне/айфоне), **синхронизация** дневника/мест/снастей между устройствами
  (js/sync.js, таблица user_data). Локальный `localStorage` — по-прежнему источник правды,
  облако — надстройка (офлайн работает без входа).
- **СООБЩЕСТВО (маховик крутится):** при сохранении улова он публикуется в общую базу
  (таблица catches — строка на КАЖДУЮ рыбу, метка trip_id, координаты огрублены).
  Вкладка **«Лента»** = **по выездам** (одна рыбалка = карточка со списком рыбы; место
  выезда, не город). Есть: **лайки выезда** (trip_likes, свой нельзя), **комментарии**
  (trip_comments), **🏆 Улов недели** (клиентский лидерборд недели), **🏆 трофей**
  (крупная рыба для вида, порог в data.js, флаг catches.trophy), **уведомления** о
  лайках/комментариях к своим уловам (js/engagement.js, клиентский диф).
- **Рейтинг рыбака «Уважение»** (вью angler_stats): очки только от людей — лайки чужих
  (+10), сбывшийся прогноз (+4), трофей (+8); всё **выцветает** (полураспад ≈83 дня),
  так «не ездишь → рейтинг сам сползает». 5 званий (Новичок…Легенда), показ 🐟×N.
  Накрутить количеством рыбы нельзя. Детали и why — память [[rybolov-community-rating]].
- **Приватность:** точные lat/lon видит только владелец (миграция 008 закрыла чтение
  анониму/другим); в ленте — огрублённые lat_pub/lon_pub через вью.
- **Ещё сделано:** приватность улова (тумблер «не публиковать»), лента-фильтры
  Все/📍Рядом/Мои (geo.js, haversine), уловы сообщества на карте (mapview.drawCatches),
  приманка в ленте и на карточке улова, **рекорды+достижения** (records.js/achievements.js),
  уведомление **«скоро жор»** (forecastAlert.js), поделиться прогнозом (postgen+Web Share),
  экспорт/импорт дневника (бэкап), PWA-install-подсказка, онбординг первого запуска.
- **Полевые фиксы (тест 4 дня):** выбор даты в дневнике (не только сегодня); тап по
  будущему дню → детальный прогноз (что клюёт в этот день); **геокодинг Nominatim/OSM**
  (деревни/сёла в поиске), reverse-geocode геолокации (реальное место, не «снап» на
  далёкий город), тап-по-карте для точной точки прогноза; **«🎣 Здесь реально ловят»**
  (nearby.js) — уловы сообщества по месту. Стратегия «рыба по водоёму» — приоритет,
  детали [[rybolov-field-feedback]] и docs/ROADMAP.md (Этап 3.5).
- **Миграции Supabase** (SQL Editor, по порядку): `schema.sql` → `003_user_data` →
  `004_community` → `007_trips` → `008_privacy_coords` → `009_comments` → `010_trophy` →
  `011_lures` → `012_realtime` → `014_review_fixes` (⚠️ реально закрывает координаты) →
  `015_feed_lure` → `016_water` → `018_reports` (жалобы на фейк/спам).
  (`013_push` + `017_push_triggers` — пуш о лайках/коммент.; `019_push_location` +
  `020_forecast_cron` + деплой `forecast-push` — пуш «завтра жор».) Все идемпотентны.
  Публикация улова устойчива к «ещё не прогнанной миграции» (insertCatchesResilient).
- **Тесты:** `npm test` = 67 проверок (движок+уверенность, лидерборд неделя/месяц/сезон,
  уведомления, приманки, скоро-жор+forecast-push, гео, рекорды+рыболовный год, вода,
  водоёмы+профиль, смоук). Чистая логика — в отдельных модулях + node-тесты.
- **Новое (эта сессия):** уверенность прогноза (score.js `forecastConfidence`);
  лидерборд с переключателем недели/месяца/сезона (`periodTop`); профиль водоёма
  (`waterProfile` — виды/приманки/лучшие месяцы, тап в списке водоёмов); виральная
  карточка «Твой рыболовный год» (`fishingYear`+`makeYearCard`); жалобы на фейк/спам
  (миграция 018, авто-скрытие при ≥3); пуш «завтра жор» (Edge `forecast-push` тем же
  движком, миграции 019/020, `tools/bundle-push.js`).
- **Развёрнуто:** https://kressent.github.io/zorka/ (GitHub Pages, репо kressent/zorka,
  авто-деплой при `git push`). Supabase проект jcazwvivxxlrhkguolfp. Вход — почта+пароль
  (тумблера Confirm-email в их дашборде НЕТ — не искать; детали [[rybolov-supabase-auth]]).
- Что нужно от пользователя — в `TODO-FOR-YOU.md` (сейчас: прогнать 009 и 010).

Предыстория: первую версию делал Claude на бесплатном тарифе (тоже веб-PWA на
Netlify, `localStorage`). Отсюда перенесены «мозги» (база рыб, правила, астрономия)
и улучшены. Старый код лежал в `C:\Users\egkir\Downloads\deploy-6a2d3ddea3e5476ab0a0b58b`.

## Архитектура и запуск
- Чистый **HTML/CSS/JS, ES-модули, без сборки**. Запуск: `node tools/serve.js` → :8080.
- Тесты (node, без браузера): `npm test` = engine + leaderboard + engagement; ещё
  `test/ui.smoke.test.js`, `test/serve.check.js`. Правило: новую чистую логику выносить
  в отдельный модуль и покрывать node-тестом (браузерные API там недоступны).
- Ключевые модули: `data.js` (виды + трофей-пороги), `score.js` (движок), `astro.js`,
  `weather.js` (Open-Meteo), `diary.js`, `tackle.js`, `locations.js`, `ui.js`, `app.js`;
  облако/сообщество: `cloud.js` (Supabase), `sync.js`, `mapview.js` (Leaflet: карта +
  пикер, гибрид-спутник с подписями), `catchcard.js`, `notify.js`.
- Чистые модули (все с node-тестами): `leaderboard.js` (Улов недели), `engagement.js`
  (уведомления о лайках/коммент.), `lurestats.js` (рейтинг приманок), `forecastAlert.js`
  (скоро жор), `geo.js` (haversine, лента «рядом»), `water.js` (сводка отметок воды),
  `records.js`/`achievements.js` (рекорды/бейджи), `nearby.js` (что ловят рядом / где
  ловится вид), `waterbodies.js` (кластеризация уловов в водоёмы). Локация — `locations.js`
  (геокодинг Nominatim/OSM: поиск сёл + reverse-geocode).
- **SW:** network-first + `cache:'reload'` (всегда свежий код). При добавлении нового
  `js/*.js` — вписать в `SHELL` в `sw.js` и поднять `VERSION`.

## Движок прогноза — как считает (score.js)
- **Балл рыбы — абсолютный** (0..5): «насколько активна ИМЕННО эта рыба сегодня».
- **Балл дня** = среднее **топ-3 активных** рыб (НЕ среднее по всем — иначе несезонные
  нули занижают день; это была ошибка старой версии «рыба 5 на дне 3»).
- Факторы: температура воды (оценка по воздуху+сезон), тренд давления, ветер (С/В
  штраф), облачность, дождь, похолодание, **нерест = 0 + предупреждение**,
  **луна** (реально влияет), **уровень/мутность воды «с памятью»** (заряд по
  60-дневной истории осадков с затуханием τ=12 дн + тренд прибывает/держится/спадает
  — ливни 2-3 нед назад держат воду высокой даже после сухой недели),
  **«жор» на сломе погоды** (падало давление → стабилизировалось = буст).
  Температура воды — с лагом по многодневной истории воздуха (тепловая инерция).
- Почасовая активность — честная: ночные виды (судак/сом/налим/лещ) дают ночные окна,
  дневные — зорьки.

## Зафиксированные продуктовые решения
- **Две аудитории:** «результатники» (важен клёв) и «процессники» (важен отдых).
  Один экран полезен обоим (клёв + погода/комфорт). Процессники — золото данных
  (ловят в любую погоду → закрывают «плохие дни» для обучения). **Без тумблера-режима.**
- **Дневник:** вес пишется **по каждой рыбе** отдельно **в граммах** (показ умный:
  до 1 кг — «г», дальше — «кг»); итог считается сам; есть режим «не взвешивал» (только
  штуки) для мелочи. Один общий вес улова — нельзя.
- **Снасти:** комплекты + учёт остатка приманок с напоминанием докупить.
- **Карта:** основной вид — **спутник** (рыбак смотрит лес/кусты/подходы/перекаты),
  переключатель Спутник/Схема, тап по карте ставит точку (можно из дома). Сейчас —
  **Leaflet** (спутник Esri + схема OSM, бесплатно, без ключей); Яндекс MapKit — опция
  на потом (нужен ключ). Флагов на карте нет (осознанно, политически нейтрально).
- **Мутность/сброс воды:** оцениваем по дождю; позже — отметки от рыбаков; ещё позже —
  официальные гидропосты. Карты глубин/эхолоты — 🔵 «на потом».
- **Монетизация (позже, не раньше пользы и ~сотен активных в регионе):** стержень —
  **affiliate** (рекомендуем «на что реально клюёт» → ссылка в магазин), подписка —
  надстройка потом, реклама — по минимуму. Не маркетплейс. Рейтинг приманок нельзя
  «продавать» — только по реальным уловам (доверие = актив).
- **Дистрибуция:** RuStore обязателен (+ Google Play/APK), офлайн-first.
- **Название:** «На крючке» (зафиксировано 2026-08-01; классы МКТУ 9/42 свободны).
  Домены `накрючке.рф`/`nakryuchke.ru` — за пользователем; ТЗ (классы 9/42) — позже,
  не блокер старта. «Зорька/Zorka» — только внутреннее кодовое имя папки/репо.

## Дизайн — «Дневной альманах» (утверждён)
Светлая бумага `#EFE9DB`, чернила-бирюза `#10322D`, нефрит `#2C6E5A`, латунь
`#C58A2E` (акцент «сейчас»/предупреждения), грифель `#5C6E64`; зерно бумаги.
Шрифты: **серифный** дисплей (Georgia/New York) для заголовков/баллов/имён рыб,
**моно** для меток, sans для текста. Формы: острые/срезанные углы (clip-path),
тонкие линейки, водное свечение (aurora) + волна-разделитель, шкала окон клёва —
подпись стиля. **Принципы:** минимализм + раскрытие по тапу; всё главное сразу;
работает с открытия; никаких «скруглённых пилюль» и фиолетовых градиентов
(признаки «AI-шаблона»). Тема — только светлая (осознанно). Тренды-обоснование:
бирюза — цвет 2026 (WGSN), уход от rounded-default (Material 3 Expressive).

## Следующие шаги — см. `docs/ROADMAP.md`
Коротко: (1) отшлифовать веб-версию по фидбэку; (2) бэкенд Supabase + аккаунты +
общая БД → **маховик данных**; (3) рейтинг приманок и лента уловов рядом;
(4) распознавание рыбы по фото (вход для данных); (5) пуши/удержание; (6) RuStore;
(7) решить native (Flutter) vs остаться PWA. Маркетинг/удержание — отдельная тема
(«хочется возвращаться перед каждой рыбалкой»).

## Полезное
- Open-Meteo запрос собирается в `weather.js:buildUrl` (past_days=2, forecast_days=7).
- Дизайн-макеты (артефакты claude.ai) утверждены пользователем: направление
  «Дневной альманах», светлый вариант.
